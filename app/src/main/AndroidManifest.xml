<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agenda">

    <application
        android:allowBackup="true"
        android:icon="@drawable/scissors"
        android:label="@string/app_name"
        android:roundIcon="@drawable/scissors"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Persons"></activity>
        <activity android:name=".Activities.Types_Customers" />
        <activity android:name=".Activities.Letex_Types" />
        <activity android:name=".Activities.Masn3_Types" />
        <activity android:name=".Activities.Types_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>